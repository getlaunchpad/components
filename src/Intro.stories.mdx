<Meta title="Design System|Intro" />

# Get started

The Launchpad design system, used to maintain relyable and reusable components throughout the brand.

## Install

Add the launchpad design system to your project.

`npm install --save @getlaunchpad/components`

### Use

Import components you want into your UI

`import { Text } from ‘@getlaunchpad/components;`

and use them like so

```js
function App() {
  return <Text type="h3">Hello world</Text>;
}
```
